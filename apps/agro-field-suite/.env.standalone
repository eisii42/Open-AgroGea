# Edizione standalone / OSS dell'app di campo.
#
# Caricata da Vite con `--mode standalone` (vedi script dev:standalone /
# build:standalone). Avvia una sessione locale fissa: nessun login, dati solo
# sul dispositivo. Logica in src/standalone.ts.
VITE_STANDALONE_MODE=true
